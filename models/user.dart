
class User {
  int studentId, userId, pin, token, phone;
  String name, email, password;

  User(this.studentId, this.userId, this.pin, this.token, this.phone,
       this.name, this.email, this.password);
}